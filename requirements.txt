# CPU prebuilt wheels for llama-cpp-python, so it installs without needing
# Visual Studio Build Tools / a C++ compiler on Windows.
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu

Flask>=3.0
pyttsx3>=2.90
pystray>=0.19
Pillow>=10.0
requests>=2.31
duckduckgo-search>=8.0
llama-cpp-python>=0.3.0
